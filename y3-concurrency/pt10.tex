\mkImg{image55}
\mkImg{image56}
\mkImg{image57}
\mkImg{image58}

\hSep

\stub{NOTE:} \stu{TSO} allows for \texttt{sfence} (with \texttt{SF} label);
and \texttt{sfence}-read reordering. The \stb{buffer} for memory transitions
can hold \texttt{SF} labels.

\hSep

\stub{NOTE:} \stu{PSO} is like \stu{TSO} but allows write-write reordering
on different memory locations.

\hSep
